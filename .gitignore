__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# runtime data — never commit
state.db
state.db-*
sources/
staged/
trash/
*.wav
*.mp3

# Local agent scratch: worktrees, launch config, per-project settings.
.claude/

.DS_Store
._*
.Spotlight-V100
.Trashes
.python-version-ci

# The designer's handoff for the main page: a prototype and two 500KB
# screenshots, kept locally as a reference. The device updates itself by
# pulling this repo, so it is not weight every Pi clone should carry.
docs/design_handoff_dittobacktracker_main/
