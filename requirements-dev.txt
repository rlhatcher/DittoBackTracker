# Dev/test dependencies. On the Pi the runtime deps come from apt
# (python3-flask, python3-waitress — see install.sh); this file lets CI and a
# local checkout install them with pip plus the test runner.
flask
waitress
pytest

# Exact, unlike the rest: a ruff minor release adds rules, so an unpinned
# linter turns a green branch red on a day nobody touched the code. Bumping it
# becomes a commit that says which new rules were adopted.
ruff==0.16.4
